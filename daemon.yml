apps:
  - script   : daemon.js
    name     : container-share
    instances: 1
    exec_mode: fork
    watch    : false
    env      :
      NODE_ENV: development
      DEBUG: container-share,modem
    env_production:
      NODE_ENV: production
